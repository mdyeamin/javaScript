var numbers = ["pan", "colorPan", "whitePan", "heilightPan", "dayri", "mobail", "mouse", "laptops"];

for (var i = 0; i < numbers.length; i++) {
    var element = numbers[i]
    console.log(element)
}